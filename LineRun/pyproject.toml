[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "linerun"
version = "0.1.0"
description = "A sandboxed Code runner"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
linerun = "linerun.main:main"
